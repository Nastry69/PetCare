knpu_oauth2_client:
    http_client: app.google_oauth_http_client
    clients:
        google:
            type: google
            client_id: '%env(GOOGLE_CLIENT_ID)%'
            client_secret: '%env(GOOGLE_CLIENT_SECRET)%'
            redirect_route: api_auth_google_callback
            redirect_params: {}
